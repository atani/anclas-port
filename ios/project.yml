name: AnclasPort
options:
  bundleIdPrefix: com.atani
  deploymentTarget:
    iOS: "17.0"
  createIntermediateGroups: true

settings:
  base:
    MARKETING_VERSION: "0.1.0"
    CURRENT_PROJECT_VERSION: "1"
    SWIFT_VERSION: "5.0"
    DEVELOPMENT_TEAM: ""

targets:
  AnclasPort:
    type: application
    platform: iOS
    sources:
      - AnclasPort
    dependencies:
      - target: AnclasPortWidgetExtension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.atani.AnclasPort
        TARGETED_DEVICE_FAMILY: "1"
        GENERATE_INFOPLIST_FILE: YES
        INFOPLIST_KEY_CFBundleDisplayName: アンクラスPort
        INFOPLIST_KEY_UILaunchScreen_Generation: YES
        INFOPLIST_KEY_UISupportedInterfaceOrientations: UIInterfaceOrientationPortrait
        ASSETCATALOG_COMPILER_APPICON_NAME: AppIcon
        ASSETCATALOG_COMPILER_GLOBAL_ACCENT_COLOR_NAME: AccentColor

  AnclasPortWidgetExtension:
    type: app-extension
    platform: iOS
    sources:
      - AnclasPortWidget
    info:
      path: AnclasPortWidget/Info.plist
      properties:
        CFBundleDisplayName: アンクラスPort Widget
        NSExtension:
          NSExtensionPointIdentifier: com.apple.widgetkit-extension
    settings:
      base:
        PRODUCT_BUNDLE_IDENTIFIER: com.atani.AnclasPort.Widget
